<!doctype html>
<html lang="ckb" dir="rtl">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <meta name="theme-color" content="#070707">
  <title>PH.Tools — داشبۆردی بەڕێوەبردن</title>
  <style>
    :root{
      --bg:#f4f4f4;
      --panel:#ffffff;
      --panel-soft:#fafafa;
      --line:#d9d9d9;
      --text:#0a0a0a;
      --muted:#666;
      --black:#090909;
      --orange:#ff7a00;
      --danger:#c92b2b;
      --radius:18px;
    }
    *{box-sizing:border-box}
    html,body{margin:0;min-height:100%;background:var(--bg);color:var(--text);font-family:Tahoma,Arial,sans-serif}
    body{min-height:100vh}
    button,input,textarea,select{font:inherit}
    button{cursor:pointer}
    button:disabled{opacity:.55;cursor:not-allowed}
    img{display:block}
    .hidden{display:none!important}
    .wrap{width:100%;max-width:1180px;margin:auto;padding:24px}
    .top{display:flex;justify-content:space-between;align-items:center;gap:18px;margin-bottom:20px}
    .brand{font-size:27px;font-weight:950;letter-spacing:-.5px}.brand span{color:var(--orange)}
    .muted{color:var(--muted);font-size:13px}
    .actions,.banner-tools,.row-actions{display:flex;gap:8px;flex-wrap:wrap}
    .btn{border:1.5px solid #111;background:#fff;color:#111;border-radius:11px;padding:10px 14px;font-weight:900;transition:.15s ease}
    .btn:hover:not(:disabled){background:#111;color:#fff}
    .btn.primary{background:#111;color:#fff;border-color:#111}.btn.primary:hover:not(:disabled){background:#292929}
    .btn.danger{background:#fff;color:#b32020;border-color:#cfcfcf}.btn.danger:hover:not(:disabled){background:#b32020;color:#fff;border-color:#b32020}
    .stats{display:grid;grid-template-columns:repeat(3,1fr);gap:12px;margin-bottom:18px}
    .stat,.panel{background:var(--panel);border:1.5px solid #111;border-radius:var(--radius);box-shadow:0 8px 24px rgba(0,0,0,.06)}
    .stat{padding:17px 18px}.stat b{display:block;font-size:30px;margin-top:6px}
    .stat-click{cursor:pointer;transition:.15s ease}.stat-click:hover{transform:translateY(-1px);box-shadow:0 12px 28px rgba(0,0,0,.09)}
    .stat.active-filter{background:#111;color:#fff}.stat.active-filter .muted{color:#ccc}
    .panel{padding:18px;margin-bottom:18px}
    .panel-head{display:flex;align-items:center;justify-content:space-between;gap:12px;margin-bottom:14px}.panel-head h2{margin:0;font-size:19px}
    .toolbar{display:flex;gap:9px;flex-wrap:wrap}.toolbar .search{flex:1;min-width:240px}.toolbar select{max-width:180px}
    .input{width:100%;background:#fff;color:#111;border:1.5px solid #bcbcbc;border-radius:11px;padding:11px 12px;outline:none}.input:focus{border-color:#111;box-shadow:0 0 0 3px rgba(0,0,0,.07)}
    textarea.input{resize:vertical}
    .products-grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:14px}
    .product-card{background:#fff;border:1.5px solid #111;border-radius:16px;overflow:hidden;transition:.16s ease}.product-card:hover{transform:translateY(-2px);box-shadow:0 12px 28px rgba(0,0,0,.09)}
    .product-image{width:100%;aspect-ratio:1/1;object-fit:cover;background:#eee;border-bottom:1.5px solid #111}.product-image.empty{display:grid;place-items:center;color:#888;font-size:13px}
    .product-body{padding:13px}.product-name{font-size:16px;font-weight:950;margin-bottom:5px}.product-price{font-size:15px;font-weight:900;margin:8px 0}.product-meta{display:flex;justify-content:space-between;align-items:center;gap:8px;margin:8px 0 12px}.product-actions{display:grid;grid-template-columns:1fr 1fr 1fr;gap:6px}.product-actions .btn{padding:9px 6px;font-size:12px}
    .badge{display:inline-flex;padding:5px 9px;border-radius:999px;font-size:11px;font-weight:900;border:1.5px solid #111}.badge.on{background:#111;color:#fff}.badge.off{background:#fff;color:#777;border-color:#bbb}
    .empty{text-align:center;padding:34px!important;color:#777;border:1.5px dashed #aaa;border-radius:14px;grid-column:1/-1}
    .login{min-height:100vh;display:grid;place-items:center;padding:20px;background:#f4f4f4}.login-card{width:min(430px,100%);background:#fff;border:1.5px solid #111;border-radius:22px;padding:25px;box-shadow:0 20px 55px rgba(0,0,0,.12)}.login-card h1{margin:14px 0 8px;font-size:23px}
    .field{margin-top:12px}.field label{display:block;font-size:12px;font-weight:900;margin-bottom:7px}.error,.ok{margin-top:12px;padding:10px 12px;border-radius:10px;line-height:1.7}.error{background:#fff0f0;color:#a90000;border:1px solid #efc4c4}.ok{background:#eff9ed;color:#28651d;border:1px solid #c8e4c0}
    .modal{position:fixed;inset:0;background:rgba(0,0,0,.72);z-index:50;display:grid;place-items:center;padding:14px}.modal-card{width:min(780px,100%);max-height:94vh;overflow:auto;background:#fff;border:1.5px solid #111;border-radius:22px;padding:20px;box-shadow:0 30px 90px rgba(0,0,0,.45);position:relative;z-index:1}
    .form-grid{display:grid;grid-template-columns:1fr 1fr;gap:11px}.full{grid-column:1/-1}.preview-grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(90px,1fr));gap:8px;margin-top:9px}.preview-grid img{width:100%;aspect-ratio:1;border-radius:11px;object-fit:cover;background:#eee}.file-row{display:flex;gap:8px;align-items:center;flex-wrap:wrap}.file-row input[type=file]{flex:1;min-width:220px}.size-row{display:grid;grid-template-columns:1fr 130px 130px auto;gap:7px;margin-top:7px}.check-row{display:flex;gap:16px;flex-wrap:wrap;margin-top:9px}.check-row label{display:flex;align-items:center;gap:7px;font-size:13px}.footer-actions{display:flex;justify-content:flex-start;gap:8px;margin-top:17px;padding-top:13px;border-top:1px solid #ddd}
    .sync-dot{display:inline-block;width:8px;height:8px;border-radius:50%;background:#999;margin-left:6px}.sync-dot.live{background:#111;box-shadow:0 0 0 4px rgba(0,0,0,.08)}
    #toast{position:fixed;bottom:18px;left:18px;z-index:100;background:#111;color:#fff;padding:11px 15px;border-radius:12px;box-shadow:0 15px 35px rgba(0,0,0,.25)}
    .banner-box{display:grid;grid-template-columns:minmax(0,1.2fr) minmax(260px,.8fr);gap:18px;align-items:start}.banner-preview{margin-top:10px;border:1.5px solid #111;border-radius:14px;overflow:hidden;background:#eee;min-height:100px;display:grid;place-items:center}.banner-preview img{width:100%;aspect-ratio:7/5;object-fit:cover}.banner-tools .btn{flex:1;min-width:170px}
    @media(max-width:900px){.products-grid{grid-template-columns:repeat(2,minmax(0,1fr))}.banner-box{grid-template-columns:1fr}}
    @media(max-width:760px){.wrap{padding:14px}.top{align-items:stretch;flex-direction:column}.actions{display:grid;grid-template-columns:1fr 1fr}.actions .btn{width:100%}.stats{grid-template-columns:1fr 1fr}.stat:last-child{grid-column:1/-1}.panel{padding:14px}.products-grid{grid-template-columns:1fr 1fr}.form-grid{grid-template-columns:1fr}.full{grid-column:auto}.modal{align-items:end;padding:0}.modal-card{width:100%;max-height:94vh;border-radius:20px 20px 0 0;padding:16px}.size-row{grid-template-columns:1fr 105px 105px auto}.toolbar{display:grid;grid-template-columns:1fr}.toolbar select,.toolbar .search{max-width:none;min-width:0}.login-card{padding:20px}}
    @media(max-width:450px){.products-grid{grid-template-columns:1fr}.product-actions{grid-template-columns:1fr 1fr 1fr}.stats{gap:8px}.stat{padding:13px}.stat b{font-size:23px}.size-row{grid-template-columns:1fr 85px 85px auto}}
  </style>
  <script type="module">
    import { initializeApp } from "https://www.gstatic.com/firebasejs/12.18.0/firebase-app.js";
    import {
      getAuth,
      onAuthStateChanged,
      signInWithEmailAndPassword,
      signOut
    } from "https://www.gstatic.com/firebasejs/12.18.0/firebase-auth.js";
    import {
      getFirestore,
      collection,
      addDoc,
      doc,
      updateDoc,
      deleteDoc,
      setDoc,
      getDoc,
      serverTimestamp,
      onSnapshot
    } from "https://www.gstatic.com/firebasejs/12.18.0/firebase-firestore.js";
    const firebaseConfig = {
      apiKey: "AIzaSyCH5-6OhuCVfZo8abiIHYfOzvbQTBQbEmw",
      authDomain: "ph-tools-4a82c.firebaseapp.com",
      projectId: "ph-tools-4a82c",
      storageBucket: "ph-tools-4a82c.firebasestorage.app",
      messagingSenderId: "677581607396",
      appId: "1:677581607396:web:7c63eee6537cdf7ccb37f1",
      measurementId: "G-DDCWBJVG1T"
    };
    const app = initializeApp(firebaseConfig, "ph-tools-admin-new");
    const auth = getAuth(app);
    const db = getFirestore(app);
    const productsRef = collection(db, "products");
    const bannerRef = doc(db, "settings", "banner");
    const $ = id => document.getElementById(id);
    let products = [];
    let unsubscribeProducts = null;
    let toastTimer = null;
    const categories = [
      ["drill", "درێل"],
      ["charger", "بابەت شەحن"],
      ["shafra", "شفرە و ماڵە"],
      ["nail", "بورغی دەرزی"],
      ["cutting", "مشار"],
      ["electronics", "ئەلیکترۆنی"],
      ["warawela", "وردەواڵە"]
    ];
    const categoryName = category =>
      categories.find(item => item[0] === category)?.[1] || category || "—";
    const escapeHtml = value =>
      String(value ?? "").replace(/[&<>"']/g, character => ({
        "&": "&amp;",
        "<": "&lt;",
        ">": "&gt;",
        '"': "&quot;",
        "'": "&#39;"
      }[character]));
    const escapeAttribute = value =>
      escapeHtml(value);
    function money(value, currency) {
      const number = Number(value || 0);
      if (currency === "USD") {
        return `$${number.toLocaleString("en-US", {
          maximumFractionDigits: 2
        })}`;
      }
      return `${number.toLocaleString("en-US")} د.ع`;
    }
    function showToast(message) {
      $("toast").textContent = message;
      $("toast").classList.remove("hidden");
      clearTimeout(toastTimer);
      toastTimer = setTimeout(() => {
        $("toast").classList.add("hidden");
      }, 2200);
    }
    async function loadBanner(){
      try {
        const snapshot = await getDoc(bannerRef);
        const url = snapshot.exists() ? String(snapshot.data()?.imageUrl || "").trim() : "";
        $("bannerUrl").value = url;
        updateBannerPreview();
        $("bannerMsg").innerHTML = url
          ? '<div class="ok">✓ بەنەرەکەی ئێستا بارکرا.</div>'
          : '<div class="muted">هیچ بەنەرێکی تایبەت پاشەکەوت نەکراوە؛ بەنەری بنەڕەتی بەکاردێت.</div>';
      } catch(error) {
        console.error("BANNER LOAD ERROR:", error);
        $("bannerMsg").innerHTML = '<div class="error">نەتوانرا بەنەر بخوێندرێتەوە.</div>';
      }
    }

    function updateBannerPreview(){
      const url = $("bannerUrl").value.trim();
      const preview = $("bannerPreview");
      preview.innerHTML = "";
      if (!url) return;
      const image = document.createElement("img");
      image.src = url;
      image.alt = "پێشبینینی بەنەر";
      image.onload = () => $("bannerPreviewHint").textContent = "پێشبینین";
      image.onerror = () => $("bannerPreviewHint").textContent = "ئەم URL ـەی وێنەکە کار ناکات.";
      preview.appendChild(image);
    }

    async function saveBanner(){
      const button = $("saveBanner");
      const url = $("bannerUrl").value.trim();
      $("bannerMsg").innerHTML = '<div class="muted">بەنەر پاشەکەوت دەکرێت...</div>';
      button.disabled = true;
      try {
        const user = auth.currentUser;
        if (!user) throw new Error("AUTH_REQUIRED");
        if (!url) throw new Error("URL_REQUIRED");
        await withTimeout(
          setDoc(bannerRef, {
            imageUrl: url,
            updatedAt: serverTimestamp(),
            updatedBy: user.uid
          }, { merge: true }),
          20000,
          "FIRESTORE_BANNER_UPDATE"
        );
        $("bannerMsg").innerHTML = '<div class="ok">✓ بەنەر بە سەرکەوتوویی نوێکرایەوە. لە ماڵپەڕەکەش خۆکارانە دەگۆڕێت.</div>';
      } catch(error) {
        console.error("BANNER SAVE ERROR:", error);
        const code = error?.code || "";
        let detail = "پاشەکەوتکردنی بەنەر سەرکەوتوو نەبوو.";
        if (error?.message === "URL_REQUIRED") detail = "تکایە URL ـی وێنەی بەنەر دابنێ.";
        else if (code === "permission-denied" || code === "PERMISSION_DENIED") detail = "Firebase ڕێگەی نووسین نادات. Firestore Rules پشکنە.";
        else if (error?.message?.startsWith("TIMEOUT:FIRESTORE_BANNER_UPDATE")) detail = "Firebase وەڵامی پاشەکەوتکردنی بەنەر نەدا.";
        $("bannerMsg").innerHTML = `<div class="error">${escapeHtml(detail)}</div>`;
      } finally {
        button.disabled = false;
      }
    }

    async function resetBanner(){
      const button = $("resetBanner");
      button.disabled = true;
      try {
        const user = auth.currentUser;
        if (!user) throw new Error("AUTH_REQUIRED");
        await withTimeout(
          setDoc(bannerRef, { imageUrl: "", updatedAt: serverTimestamp(), updatedBy: user.uid }, { merge: true }),
          20000,
          "FIRESTORE_BANNER_RESET"
        );
        $("bannerUrl").value = "";
        updateBannerPreview();
        $("bannerMsg").innerHTML = '<div class="ok">✓ گەڕێندرایەوە بۆ بەنەری بنەڕەتی.</div>';
      } catch(error) {
        console.error("BANNER RESET ERROR:", error);
        $("bannerMsg").innerHTML = '<div class="error">نەتوانرا بەنەر بگەڕێندرێتەوە.</div>';
      } finally {
        button.disabled = false;
      }
    }

    function showLogin() {
      $("loginView").classList.remove("hidden");
      $("appView").classList.add("hidden");
    }
    function showApp() {
      $("loginView").classList.add("hidden");
      $("appView").classList.remove("hidden");
    }
    function setSyncState(live, text) {
      $("syncState").innerHTML = `
        <span class="sync-dot${live ? " live" : ""}"></span>
        ${escapeHtml(text)}
      `;
    }
    function render() {
      const query = $("search").value.trim().toLowerCase();
      const status = $("status").value;
      const category = $("categoryFilter").value;
      const filteredProducts = products.filter(product => {
        const searchableText = [product.name, product.brand, product.category, product.smallDescription].join(" ").toLowerCase();
        if (query && !searchableText.includes(query)) return false;
        if (category !== "all" && product.category !== category) return false;
        if (status === "active" && product.active === false) return false;
        if (status === "inactive" && product.active !== false) return false;
        return true;
      });
      $("total").textContent = products.length;
      $("activeCount").textContent = products.filter(product => product.active !== false).length;
      $("inactiveCount").textContent = products.filter(product => product.active === false).length;
      const container = $("rows");
      if (!filteredProducts.length) {
        container.innerHTML = `<div class="empty">هیچ بابەتێک نییە.</div>`;
        return;
      }
      container.innerHTML = filteredProducts.map(product => {
        const image = Array.isArray(product.images) && product.images[0] ? product.images[0] : product.imageUrl || "";
        const price = product.sizes?.length ? money(product.sizes[0].price, product.sizes[0].currency) : money(product.price, product.currency);
        const active = product.active !== false;
        return `
          <article class="product-card">
            ${image
              ? `<img class="product-image" src="${escapeAttribute(image)}" alt="${escapeAttribute(product.name || "")}" loading="lazy" referrerpolicy="no-referrer">`
              : `<div class="product-image empty">وێنە نییە</div>`}
            <div class="product-body">
              <div class="product-name">${escapeHtml(product.name || "بێ ناو")}</div>
              <div class="muted">${escapeHtml(categoryName(product.category))}${product.brand ? ` · ${escapeHtml(product.brand)}` : ""}</div>
              <div class="product-price">${price}</div>
              <div class="product-meta">
                <span class="badge ${active ? "on" : "off"}">${active ? "چالاک" : "ناچالاک"}</span>
                <span class="muted">ستۆک: ${Number(product.stock || 0)}</span>
              </div>
              <div class="product-actions">
                <button class="btn" type="button" data-action="edit" data-id="${escapeAttribute(product.id)}">دەستکاری</button>
                <button class="btn ${active ? "danger" : "primary"}" type="button" data-action="toggle" data-id="${escapeAttribute(product.id)}">${active ? "ناچالاک" : "چالاک"}</button>
                <button class="btn danger" type="button" data-action="delete" data-id="${escapeAttribute(product.id)}">سڕینەوە</button>
              </div>
            </div>
          </article>`;
      }).join("");
    }
    function addSizeRow(size = {}) {
      const row = document.createElement("div");
      row.className = "size-row";
      row.innerHTML = `
        <input
          class="input sz-name"
          placeholder="قەبارە"
          value="${escapeAttribute(size.name || "")}"
        >
        <input
          class="input sz-price"
          type="number"
          min="0"
          step="0.01"
          placeholder="نرخ"
          value="${escapeAttribute(size.price ?? "")}"
        >
        <select class="input sz-cur">
          <option
            value="IQD"
            ${size.currency === "IQD" || !size.currency ? "selected" : ""}
          >
            IQD
          </option>
          <option
            value="USD"
            ${size.currency === "USD" ? "selected" : ""}
          >
            USD
          </option>
        </select>
        <button
          type="button"
          class="btn danger"
          data-action="remove-size"
          aria-label="سڕینەوەی قەبارە"
        >
          ×
        </button>
      `;
      $("sizes").appendChild(row);
    }
    function readSizes() {
      return [...$("sizes").children]
        .map(row => ({
          name: row.querySelector(".sz-name").value.trim(),
          price: Number(row.querySelector(".sz-price").value || 0),
          currency: row.querySelector(".sz-cur").value
        }))
        .filter(size => size.name);
    }
    function addPreview(url) {
      const image = document.createElement("img");
      image.src = url;
      image.alt = "";
      image.loading = "lazy";
      image.referrerPolicy = "no-referrer";
      image.onerror = () => {
        image.remove();
      };
      $("preview").appendChild(image);
    }
    function openModal(product = null) {
      $("form").reset();
      $("id").value = product?.id || "";
      $("title").textContent =
        product ? "دەستکاریی بەرهەم" : "زیادکردنی بەرهەم";
      $("active").checked =
        product ? product.active !== false : true;
      $("featured").checked =
        Boolean(product?.featured);
      $("currency").value =
        product?.currency || "IQD";
      $("category").value =
        product?.category || "drill";
      $("stock").value =
        product?.stock ?? 0;
      $("price").value =
        product?.price ?? 0;
      $("images").value =
        Array.isArray(product?.images)
          ? product.images.join("\n")
          : "";
      $("preview").innerHTML = "";
      $("sizes").innerHTML = "";
      $("msg").innerHTML = "";
      if (product) {
        $("name").value = product.name || "";
        $("brand").value = product.brand || "";
        $("small").value = product.smallDescription || "";
        $("description").value = product.description || "";
        $("video").value = product.videoUrl || product.video || "";
        (product.images || []).forEach(addPreview);
        (product.sizes || []).forEach(addSizeRow);
      }
      $("modal").classList.remove("hidden");
      $("name").focus();
    }
    function closeModal() {
      $("modal").classList.add("hidden");
      $("msg").innerHTML = "";
    }
    function withTimeout(promise, milliseconds, label) {
      return Promise.race([
        promise,
        new Promise((_, reject) => {
          setTimeout(
            () => reject(new Error(`TIMEOUT:${label}`)),
            milliseconds
          );
        })
      ]);
    }
    function getProductById(id) {
      return products.find(product => product.id === id);
    }
    async function saveProduct(event) {
      event.preventDefault();
      const button = $("save");
      button.disabled = true;
      button.textContent = "پاشەکەوت دەکرێت...";
      $("msg").innerHTML =
        '<div class="muted">تکایە چاوەڕێ بکە...</div>';
      try {
        const user = auth.currentUser;
        if (!user) {
          throw new Error("AUTH_REQUIRED");
        }
        const name = $("name").value.trim();
        if (!name) {
          throw new Error("NAME_REQUIRED");
        }
        const images = $("images")
          .value
          .split(/\r?\n/)
          .map(url => url.trim())
          .filter(Boolean);
        const payload = {
          name,
          price: Number($("price").value || 0),
          currency: $("currency").value,
          category: $("category").value,
          brand: $("brand").value.trim(),
          smallDescription: $("small").value.trim(),
          description: $("description").value.trim(),
          images,
          videoUrl: $("video").value.trim(),
          sizes: readSizes(),
          stock: Number($("stock").value || 0),
          featured: $("featured").checked,
          active: $("active").checked,
          imageUrl: images[0] || "",
          updatedAt: serverTimestamp()
        };
        const id = $("id").value.trim();
        if (id) {
          await withTimeout(
            updateDoc(doc(db, "products", id), payload),
            20000,
            "FIRESTORE_UPDATE"
          );
          showToast("بەرهەمەکە نوێکرایەوە");
        } else {
          payload.createdAt = serverTimestamp();
          await withTimeout(
            addDoc(productsRef, payload),
            20000,
            "FIRESTORE_CREATE"
          );
          showToast("بەرهەمەکە زیادکرا");
        }
        $("msg").innerHTML =
          '<div class="ok">✓ بە سەرکەوتوویی پاشەکەوت کرا.</div>';
        setTimeout(closeModal, 500);
      } catch (error) {
        console.error("PRODUCT SAVE ERROR:", error);
        const code = error?.code || "";
        const message = error?.message || String(error);
        let detail = "";
        if (
          code === "permission-denied" ||
          code === "PERMISSION_DENIED"
        ) {
          detail =
            "Firebase ڕێگەی نووسین نادات. Firestore Rules پشکنە.";
        } else if (message === "AUTH_REQUIRED") {
          detail = "تکایە دووبارە login بکە.";
        } else if (message === "NAME_REQUIRED") {
          detail = "ناوی بەرهەم پڕبکەرەوە.";
        } else if (
          message.startsWith("TIMEOUT:FIRESTORE_UPDATE")
        ) {
          detail =
            "نوێکردنەوەی بەرهەم وەڵامی Firebase وەرنەگرت. زۆرجار ئەمە لە Firestore Rules یان پەیوەندی Firebase ـە.";
        } else if (
          message.startsWith("TIMEOUT:FIRESTORE_CREATE")
        ) {
          detail =
            "زیادکردنی بەرهەم وەڵامی Firebase وەرنەگرت. زۆرجار ئەمە لە Firestore Rules یان پەیوەندی Firebase ـە.";
        } else {
          detail =
            `هەڵە: ${code || "UNKNOWN"} — ${message}`;
        }
        $("msg").innerHTML = `
          <div class="error">
            پاشەکەوتکردن سەرکەوتوو نەبوو.
            <br>
            ${escapeHtml(detail)}
            <br>
            <small>
              کۆدی هەڵە:
              ${escapeHtml(code || "none")}
            </small>
          </div>
        `;
      } finally {
        button.disabled = false;
        button.textContent = "پاشەکەوتکردن";
      }
    }
    async function removeProduct(id) {
      const product = getProductById(id);
      if (!product) {
        return;
      }
      const confirmed = confirm(
        `دڵنیایت لە سڕینەوەی «${product.name}»؟`
      );
      if (!confirmed) {
        return;
      }
      try {
        await deleteDoc(doc(db, "products", id));
        showToast("سڕایەوە");
      } catch (error) {
        console.error("PRODUCT DELETE ERROR:", error);
        showToast("سڕینەوە سەرکەوتوو نەبوو");
      }
    }
    async function toggleProduct(id) {
      const product = getProductById(id);
      if (!product) {
        return;
      }
      const newActiveState =
        product.active === false;
      try {
        await updateDoc(
          doc(db, "products", id),
          {
            active: newActiveState,
            updatedAt: serverTimestamp()
          }
        );
        showToast(
          newActiveState
            ? "چالاک کرا"
            : "ناچالاک کرا"
        );
      } catch (error) {
        console.error("PRODUCT STATUS ERROR:", error);
        showToast("گۆڕینی دۆخ سەرکەوتوو نەبوو");
      }
    }
    function subscribeToProducts() {
      if (unsubscribeProducts) {
        unsubscribeProducts();
        unsubscribeProducts = null;
      }
      setSyncState(false, "چاوەڕوانی sync...");
      unsubscribeProducts = onSnapshot(
        productsRef,
        snapshot => {
          products = snapshot.docs.map(document => ({
            id: document.id,
            ...document.data()
          }));
          setSyncState(true, "هاوکاتە");
          render();
        },
        error => {
          console.error("PRODUCT SYNC ERROR:", error);
          setSyncState(false, "کێشەی sync");
        }
      );
    }
    function handleProductAction(event) {
      const button = event.target.closest("[data-action]");
      if (!button) {
        return;
      }
      const action = button.dataset.action;
      const id = button.dataset.id;
      if (action === "edit") {
        const product = getProductById(id);
        if (product) {
          openModal(product);
        }
        return;
      }
      if (action === "toggle") {
        toggleProduct(id);
        return;
      }
      if (action === "delete") {
        removeProduct(id);
      }
    }
    function handleSizeAction(event) {
      const button = event.target.closest(
        '[data-action="remove-size"]'
      );
      if (!button) {
        return;
      }
      button.closest(".size-row")?.remove();
    }
    function updateImagePreview() {
      const urls = $("images")
        .value
        .split(/\r?\n/)
        .map(url => url.trim())
        .filter(Boolean);
      $("preview").innerHTML = "";
      urls.forEach(addPreview);
    }
    function setupEventListeners() {
      $("loginBtn").addEventListener("click", async () => {
        const email = $("email").value.trim();
        const password = $("password").value;
        $("loginMsg").innerHTML = "";
        if (!email || !password) {
          $("loginMsg").innerHTML = `
            <div class="error">
              ئیمەیڵ و وشەی نهێنی پڕبکەرەوە.
            </div>
          `;
          return;
        }
        const button = $("loginBtn");
        button.disabled = true;
        button.textContent = "چاوەڕوان بە...";
        try {
          await signInWithEmailAndPassword(
            auth,
            email,
            password
          );
        } catch (error) {
          console.error("LOGIN ERROR:", error);
          $("loginMsg").innerHTML = `
            <div class="error">
              چوونەژوورەوە سەرکەوتوو نەبوو.
            </div>
          `;
        } finally {
          button.disabled = false;
          button.textContent = "چوونەژوورەوە";
        }
      });
      $("password").addEventListener("keydown", event => {
        if (event.key === "Enter") {
          $("loginBtn").click();
        }
      });
      $("logout").addEventListener("click", () => {
        signOut(auth).catch(error => {
          console.error("LOGOUT ERROR:", error);
        });
      });
      $("refresh").addEventListener("click", render);
      $("bannerUrl").addEventListener("input", updateBannerPreview);
      $("saveBanner").addEventListener("click", saveBanner);
      $("resetBanner").addEventListener("click", resetBanner);
      $("add").addEventListener("click", () => {
        openModal();
      });
      $("addTop").addEventListener("click", () => {
        openModal();
      });
      $("activeStat").addEventListener("click", () => {
        $("status").value = $("status").value === "active" ? "all" : "active";
        render();
      });
      $("inactiveStat").addEventListener("click", () => {
        $("status").value = $("status").value === "inactive" ? "all" : "inactive";
        render();
      });
      $("search").addEventListener("input", render);
      $("categoryFilter").addEventListener("change", render);
      $("status").addEventListener("change", render);
      $("close").addEventListener("click", closeModal);
      $("cancel").addEventListener("click", closeModal);
      $("bg").addEventListener("click", closeModal);
      $("form").addEventListener("submit", saveProduct);
      $("addSize").addEventListener("click", () => {
        addSizeRow();
      });
      $("rows").addEventListener(
        "click",
        handleProductAction
      );
      $("sizes").addEventListener(
        "click",
        handleSizeAction
      );
      $("images").addEventListener(
        "input",
        updateImagePreview
      );
      $("files").addEventListener("change", () => {
        const count = $("files").files.length;
        $("fileHint").textContent = count
          ? "تەنها URL ـی وێنە پشتیوانی دەکرێت؛ فایلەکان پاشەکەوت ناکرێن"
          : "URL ـی وێنە لە خوارەوە دابنێ";
        $("files").value = "";
      });
      document.addEventListener("keydown", event => {
        if (
          event.key === "Escape" &&
          !$("modal").classList.contains("hidden")
        ) {
          closeModal();
        }
      });
    }
    function initialize() {
      setupEventListeners();
      onAuthStateChanged(auth, user => {
        if (!user) {
          showLogin();
          if (unsubscribeProducts) {
            unsubscribeProducts();
            unsubscribeProducts = null;
          }
          products = [];
          render();
          return;
        }
        showApp();
        loadBanner();
        subscribeToProducts();
      });
    }
    initialize();
  </script>
</head>
<body>
  <!-- LOGIN -->
  <div id="loginView" class="login">
    <div class="login-card">
      <div class="brand">
        PH<span>.</span>Tools
      </div>
      <h1>چوونەژوورەوەی ئەدمین</h1>
      <div class="muted">
        بەڕێوەبردنی بەرهەمەکان و sync ـی ڕاستەوخۆ لەگەڵ ماڵپەڕ.
      </div>
      <div class="field">
        <label for="email">ئیمەیڵ</label>
        <input
          id="email"
          class="input"
          type="email"
          autocomplete="username"
        >
      </div>
      <div class="field">
        <label for="password">وشەی نهێنی</label>
        <input
          id="password"
          class="input"
          type="password"
          autocomplete="current-password"
        >
      </div>
      <button
        id="loginBtn"
        class="btn primary"
        type="button"
        style="width:100%;margin-top:15px"
      >
        چوونەژوورەوە
      </button>
      <div id="loginMsg"></div>
    </div>
  </div>
  <!-- APP -->
  <div id="appView" class="hidden">
    <div class="wrap">
      <div class="top">
        <div>
          <div class="brand">PH<span>.</span>Tools</div>
          <div class="muted">داشبۆردی بەڕێوەبردن · <span id="syncState"><span class="sync-dot"></span>چاوەڕوانی sync...</span></div>
        </div>
        <div class="actions">
          <button id="refresh" class="btn" type="button">نوێکردنەوە</button>
          <button id="add" class="btn primary" type="button">+ زیادکردنی بابەت</button>
          <button id="logout" class="btn" type="button">چوونەدەرەوە</button>
        </div>
      </div>

      <div class="stats">
        <div class="stat"><div class="muted">بابەتەکان</div><b id="total">0</b></div>
        <div id="activeStat" class="stat stat-click"><div class="muted">چالاک</div><b id="activeCount">0</b></div>
        <div id="inactiveStat" class="stat stat-click"><div class="muted">ناچالاک</div><b id="inactiveCount">0</b></div>
      </div>

      <div class="panel">
        <div class="panel-head">
          <div><h2>بەنەر</h2><div class="muted">بەنەری سەرەکیی ماڵپەڕ لێرەوە بگۆڕە.</div></div>
        </div>
        <div class="banner-box">
          <div>
            <div class="field">
              <label for="bannerUrl">Link ـی بەنەر</label>
              <input id="bannerUrl" class="input" type="url" placeholder="https://.../banner.webp">
            </div>
            <div class="banner-tools">
              <button id="saveBanner" class="btn primary" type="button">Update</button>
              <button id="resetBanner" class="btn" type="button">Restore Previous banner</button>
            </div>
            <div id="bannerMsg"></div>
          </div>
          <div>
            <div id="bannerPreviewHint" class="muted">پێشبینین</div>
            <div id="bannerPreview" class="banner-preview"></div>
          </div>
        </div>
      </div>

      <div class="panel">
        <div class="panel-head">
          <div><h2>بابەتەکان</h2><div class="muted">هەموو بەرهەمەکانت لێرە بە شێوەی کارت دەبینیت.</div></div>
          <button id="addTop" class="btn primary" type="button">+ زیادکردنی بابەت</button>
        </div>
        <div class="toolbar">
          <input id="search" class="input search" type="search" placeholder="گەڕان بە ناو، براند یان کەتەگۆری..." autocomplete="off">
          <select id="categoryFilter" class="input">
            <option value="all">هەموو کەتەگۆرییەکان</option>
            <option value="drill">درێل</option><option value="charger">بابەت شەحن</option><option value="shafra">شفرە و ماڵە</option><option value="nail">بورغی دەرزی</option><option value="cutting">مشار</option><option value="electronics">ئەلیکترۆنی</option><option value="warawela">وردەواڵە</option>
          </select>
          <select id="status" class="input">
            <option value="all">هەموو دۆخەکان</option><option value="active">چالاک</option><option value="inactive">ناچالاک</option>
          </select>
        </div>
      </div>

      <div id="rows" class="products-grid"></div>
    </div>
  </div>
  <!-- PRODUCT MODAL -->
  <div id="modal" class="modal hidden">
    <div
      id="bg"
      style="position:absolute;inset:0"
    ></div>
    <div class="modal-card">
      <div class="panel-head">
        <h2 id="title">
          زیادکردنی بەرهەم
        </h2>
        <button
          id="close"
          class="btn"
          type="button"
          aria-label="داخستن"
        >
          ×
        </button>
      </div>
      <form id="form">
        <input id="id" type="hidden">
        <div class="form-grid">
          <!-- NAME -->
          <div class="field">
            <label for="name">
              ناوی پیشاندان *
            </label>
            <input
              id="name"
              class="input"
              type="text"
              required
            >
          </div>
          <!-- PRICE -->
          <div class="field">
            <label for="price">
              نرخی سەرەکی *
            </label>
            <input
              id="price"
              class="input"
              type="number"
              min="0"
              step="0.01"
              required
            >
          </div>
          <!-- CURRENCY -->
          <div class="field">
            <label for="currency">
              دراو
            </label>
            <select id="currency" class="input">
              <option value="IQD">
                IQD — دینار
              </option>
              <option value="USD">
                USD — دۆلار
              </option>
            </select>
          </div>
          <!-- CATEGORY -->
          <div class="field">
            <label for="category">
              کەتەگۆری
            </label>
            <select id="category" class="input" required>
              <option value="drill" selected>
                درێل
              </option>
              <option value="charger">
                بابەت شەحن
              </option>
              <option value="shafra">
                شفرە و ماڵە
              </option>
              <option value="nail">
                بورغی دەرزی
              </option>
              <option value="cutting">
                مشار
              </option>
              <option value="electronics">
                ئەلیکترۆنی
              </option>
              <option value="warawela">
                وردەواڵە
              </option>
            </select>
          </div>
          <!-- BRAND -->
          <div class="field">
            <label for="brand">
              براند
            </label>
            <input
              id="brand"
              class="input"
              type="text"
            >
          </div>
          <!-- STOCK -->
          <div class="field">
            <label for="stock">
              ستۆک
            </label>
            <input
              id="stock"
              class="input"
              type="number"
              min="0"
              value="0"
            >
          </div>
          <!-- SHORT DESCRIPTION -->
          <div class="field full">
            <label for="small">
              وەسفی کورت
            </label>
            <textarea
              id="small"
              class="input"
              rows="2"
            ></textarea>
          </div>
          <!-- IMAGES -->
          <div class="field full">
            <label for="images">
              وێنەکان
            </label>
            <div class="file-row">
              <input
                id="files"
                class="input"
                type="file"
                accept="image/*"
                multiple
              >
              <span id="fileHint" class="muted">
                تەنها URL ـی وێنە بەکاربهێنە؛ Firebase Storage پێویست نییە.
              </span>
            </div>
            <textarea
              id="images"
              class="input"
              rows="3"
              style="margin-top:8px"
              placeholder="یان URL ـی وێنەکان، هەر URL لە هێڵێکی جیاواز"
            ></textarea>
            <div
              id="preview"
              class="preview-grid"
            ></div>
          </div>
          <!-- VIDEO -->
          <div class="field full">
            <label for="video">
              ڤیدیۆ (URL)
            </label>
            <input
              id="video"
              class="input"
              type="url"
              placeholder="YouTube / Vimeo / direct video URL"
            >
          </div>
          <!-- DESCRIPTION -->
          <div class="field full">
            <label for="description">
              وەسفی تەواو
            </label>
            <textarea
              id="description"
              class="input"
              rows="5"
            ></textarea>
          </div>
          <!-- SIZES -->
          <div class="field full">
            <div
              class="panel-head"
              style="margin:0 0 6px"
            >
              <label style="margin:0">
                قەبارە و نرخە جیاوازەکان
              </label>
              <button
                id="addSize"
                type="button"
                class="btn"
              >
                + قەبارە
              </button>
            </div>
            <div id="sizes"></div>
          </div>
          <!-- FLAGS -->
          <div class="field full">
            <div class="check-row">
              <label>
                <input
                  id="featured"
                  type="checkbox"
                >
                نیشانکراو
              </label>
              <label>
                <input
                  id="active"
                  type="checkbox"
                >
                چالاک
              </label>
            </div>
          </div>
        </div>
        <div id="msg"></div>
        <!-- ACTIONS -->
        <div class="footer-actions">
          <button
            id="cancel"
            type="button"
            class="btn"
          >
            هەڵوەشاندنەوە
          </button>
          <button
            id="save"
            type="submit"
            class="btn primary"
          >
            پاشەکەوتکردن
          </button>
        </div>
      </form>
    </div>
  </div>
  <!-- TOAST -->
  <div
    id="toast"
    class="hidden"
    role="status"
    aria-live="polite"
  ></div>
</body>
</html>
